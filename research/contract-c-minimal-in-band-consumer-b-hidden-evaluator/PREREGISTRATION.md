# Contract C minimal in-band Consumer B hidden evaluator preregistration

**Classification:** supervisor-only post-freeze comparison apparatus. Do not expose this branch, file, evaluator logic, expected mutations, or results to the context-free implementer before candidate freeze.

This is not production authorization, Contract C promotion, version assignment, release, tag, merge, Contract E work, or execution.

## Exact supervisor base

- Decision Engine protected `main`: `7be709b2141c767c5da89b8b94cf90233c4238fe`
- clean-room repository: `camerontjs-dot/research-scaffold-harness`
- exact aperture commit: `cb1d27ff00ae030093c5e97d78477cec7f20c6f4`
- exact sparse aperture tree: `2fade6a19f1e199fd5d3412480badc3eac141eb2`
- execution branch initially points exactly to the aperture commit
- frozen handoff blob: `14e88cbc691f7eba9366b4bf88611ef834637f27`
- frozen handoff whole-object SHA-256: `sha256:325962ebcdbf6af836bb6193a451524ccd40b4d10f2394ff9f703fbfce1ec1e3`
- frozen result-set ID: `result-set:4483272c4f6fbd9cb2362be7e3174bbd00aff3cf761d6c374897f3478818c9f0`

## Question

After an independently implemented Consumer B is frozen without this evaluator, does it satisfy the public aperture semantics and preserve the minimized in-band Contract C invariant set under adversarial and metamorphic comparison?

## Comparison boundary

The comparison may begin only after the supervisor verifies a clean pre-freeze receipt and exact candidate commit.

The candidate must descend from exact aperture commit `cb1d27ff00ae030093c5e97d78477cec7f20c6f4` and may differ from it only under `candidate/`.

The evaluator must not require any contribution-ID derivation formula absent from the public aperture. Contribution IDs are tested as typed stable identities plus uniqueness/reference closure only.

## Preregistered comparison families

The post-freeze comparison will check:

1. exact baseline recovery of the frozen neutral causal attribution and multiplicity;
2. narrow supported-claim policy remains HOLD for the baseline;
3. external exact-object authority is required and cannot be self-selected by the object;
4. wrong version, Contract-B binding, proposition binding, evidence reference, stale result identity, missing/overlapping/unclassified contribution participation, and malformed causal cardinality fail closed when the mutated object is otherwise re-bound coherently enough to reach the intended discriminator;
5. `non_deciding -> support` and `non_deciding -> counterevidence` are observable semantic changes, not silently normalized back to neutral, while the unchanged `not_checkable` verdict still does not CLEAR;
6. the same evidence members under `jointly_sufficient` remain observably distinct from `independent_sufficient_alternatives`;
7. causal versus residual neutral participation remains observably distinct;
8. a coherent deletion with recomputed internal result identity is still rejected under the original externally authorized whole-object digest;
9. no sidecar-specific member/state/receipt layer and no scalar/confidence/probability/rank/winner surface is required;
10. a weak implementation equivalent to `causal non_deciding => CLEAR` is killed by the baseline policy expectation.

## Evaluator validity controls

- Mutations intended to test semantic/reference validity receive recomputed internal `result_set_id` and a matching newly supplied external object digest so a whole-object mismatch cannot trivially mask the intended check.
- Mutations intended to test external object authority deliberately retain the original authorized digest.
- Metamorphic cases expected to remain valid use only transformations authorized by the public aperture.
- The evaluator records all mismatches before terminal classification where practical.

## Terminal states

- `SUPPORTED_CONTEXT_FREE_CONSUMER_B_REPRODUCTION`
- `FALSIFIED_CONTEXT_FREE_CONSUMER_B_REPRODUCTION`
- `INCONCLUSIVE_EVALUATOR_INVALID`
- `CONTAMINATED_EXECUTION`
- `BLOCKED`

A green workflow alone is not promotion authority. The resulting research record remains Draft evidence until supervisor reconciliation.
