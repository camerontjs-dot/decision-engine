# R2 correction

Preserved predecessor runs on head `66ce579ba98ed1c93a2556f3418d25f20a9bb31e` reached the scientific runner after all exact authority and frozen-kernel checks, then failed before Decision evaluation with `manifest_noncanonical_order`.

The defect was internal to the research manifest producer/validator pair: `buildManifestValue` sorted entry paths with locale-sensitive `localeCompare`, while `admitManifest` tested canonical order using JavaScript's default code-unit `.sort()` order. The baseline authority therefore rejected its own generated bytes.

R2 changes only the producer's entry-path comparator to explicit locale-independent code-unit ordering, consistent with the validator. It does not alter:

- the exact PR #58 projection-kernel bytes;
- either manifest Decision policy;
- source-bound admission semantics;
- target-resolution semantics;
- preregistered cases or expected scientific outcomes;
- Contract C or Contract D authority.

The predecessor failure remains an evaluator/apparatus defect, not evidence for or against the Decision projection seam.
