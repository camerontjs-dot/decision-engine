# Valid CAL abstention -> Decision behavior

## Classification

Draft Research Infrastructure / bounded integration experiment. No production authorization, merge, release, tag, promotion, Contract C/D change, Authorization, or execution.

## Exact base

Decision Engine `main`: `a4425f8eb47449ff6c683222921bbea9483742e2`.

## Parent evidence

- Draft PR #41: fresh CAL-produced supported Contract C interoperates with both maintained Decision policies.
- Draft PR #42: a fresh CAL-produced contradicted Contract C makes supported-claim verification HOLD while causal-basis citation CLEARs the exact deciding counterevidence contribution.

This successor tests the first valid abstention state.

## Controlled intervention

The frozen pipeline fixture already contains `PIPE-P3`: `Women and Men both appeared in the annual report.` It is normally rejected for child 1 while the actual strict-comparison passage `PIPE-P1` is accepted.

Change only child 1 admission:

- baseline accepted: `PIPE-P1`
- baseline rejected: `PIPE-P3`
- variant accepted: `PIPE-P3`
- variant rejected: `PIPE-P1`

Keep the claim/target, corpus, EB profile, CAL semantic machinery, and Contract authorities fixed. CAL must construct and validate Contract C itself.

## Preregistered hypothesis

The accepted P3 passage contains the target entities but no strict comparison. Expected upstream behavior is safe semantic abstention and valid Contract C `not_checkable`.

Decision expectations:

1. supported-claim verification must not CLEAR; expected disposition is HOLD because `not_checkable` is not `supported`;
2. the Contract C object should expose no deciding contribution in `basis_members`;
3. therefore the maintained causal-basis citation target helper should have no exact deciding contribution target to nominate;
4. any emitted claim-policy Contract D must validate exactly under Contract D 1.0 authority.

## Falsifiers

- CAL manufactures support/refutation from P3;
- Contract C validation fails for the safe abstention;
- supported-claim verification CLEARs `not_checkable`;
- a deciding contribution appears despite CAL abstention;
- maintained Decision source must change to achieve safe behavior.

## Boundary

This does not claim that every abstention has zero residual contributions, nor does it establish source legitimacy, corpus completeness, Authorization, or execution.
