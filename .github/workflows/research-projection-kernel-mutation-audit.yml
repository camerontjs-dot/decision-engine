name: Research - Projection kernel mutation audit

on:
  push:
    branches:
      - research/projection-kernel-mutation-audit-20260910
  pull_request:
    branches:
      - research/contract-c-adapter-projection-kernel-20260909-r2
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mutation-audit:
    runs-on: ubuntu-24.04
    env:
      SUBJECT_RECORD_HEAD: 00251c6d01a45f6cb14fc3d8ac4c7a4fb566f57c
      SUBJECT_KERNEL_SHA: e12d53020b3a6a046f4ee7688db0a5ea5c5e0a63e758b1c1e5711e7b227759e3
      CONTRACT_D_RELEASE: 298a1a0f7b7b6d7712e11200d04faec3e1ca169b
      CONTRACT_D_TAG: contract-d-v1.0.0
      CONTRACT_D_TAG_OBJECT: 6eadd688b482f3c9fce2ce5e7a2841089d852096
    steps:
      - name: Checkout mutation branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout exact Contract D authority
        uses: actions/checkout@v4
        with:
          repository: camerontjs-dot/apparatus-contracts
          ref: ${{ env.CONTRACT_D_RELEASE }}
          path: .contract-d
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install exact Contract D validator dependencies
        run: python -m pip install --disable-pip-version-check -e './.contract-d[dev]'

      - name: Verify exact subject lineage and authority
        shell: bash
        run: |
          set -euo pipefail
          git merge-base --is-ancestor "$SUBJECT_RECORD_HEAD" HEAD
          test "$(git -C .contract-d rev-parse HEAD)" = "$CONTRACT_D_RELEASE"
          test "$(git -C .contract-d rev-parse "refs/tags/$CONTRACT_D_TAG")" = "$CONTRACT_D_TAG_OBJECT"
          test "$(git -C .contract-d rev-parse "$CONTRACT_D_TAG^{}")" = "$CONTRACT_D_RELEASE"
          test "$(sha256sum research/contract-c-adapter-projection-kernel/projectionKernel.mjs | cut -d' ' -f1)" = "$SUBJECT_KERNEL_SHA"

      - name: Execute mutation audit
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .projection-mutation-output
          mkdir -p .projection-mutation-output
          node research/projection-kernel-mutation-audit/run.mjs \
            --contract-d-root "$PWD/.contract-d" \
            --out "$PWD/.projection-mutation-output" | tee .projection-mutation-output/console.log

      - name: Verify research-only mutation surface
        shell: bash
        run: |
          set -euo pipefail
          changed="$(git diff --name-only "$SUBJECT_RECORD_HEAD"...HEAD)"
          printf '%s\n' "$changed"
          if printf '%s\n' "$changed" | grep -Eq '^(src|scripts|tests)/'; then
            echo 'Mutation audit modified maintained Decision paths' >&2
            exit 1
          fi

      - name: Package evidence
        shell: bash
        run: |
          set -euo pipefail
          tar -czf projection-kernel-mutation-audit-evidence.tgz \
            .projection-mutation-output \
            research/projection-kernel-mutation-audit
          sha256sum projection-kernel-mutation-audit-evidence.tgz | tee .projection-mutation-output/evidence-tgz.sha256

      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: projection-kernel-mutation-audit-${{ github.run_id }}
          path: |
            .projection-mutation-output/
            projection-kernel-mutation-audit-evidence.tgz
          if-no-files-found: error
          retention-days: 90
