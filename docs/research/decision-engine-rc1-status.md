# Decision Engine RC1 Status

**Current status:** consumer/runtime experiment executed; production unchanged.

The RC1 branch now contains:

- four frozen Contract-C-like consumer policies;
- a research-only neutral policy/receipt runtime;
- an adapter exercising the current production Gate;
- mutation, ablation, unknown, malformed/system-failure, replay, invariance, and action-separation tests;
- a separate table-oriented publication implementation;
- build-versus-adopt analysis;
- a preserved first-run apparatus failure/deviation;
- an explicit final research disposition.

Current bounded conclusion:

- career Select/Rank remains separate;
- the Gate's named-criterion/no-score/fail-versus-unknown core is reusable;
- `promote | hold | reject` is not generic enough without policy-specific action mapping;
- `not_applicable` and system failure require semantics beyond the current Gate;
- no substantial bespoke generalized Decision Engine is justified by current evidence.

**Disposition:** NEEDS ITERATION.

No production runtime rewrite or merge is authorized.
